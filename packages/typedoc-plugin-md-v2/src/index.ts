import type { MarkdownApplication } from 'typedoc-plugin-markdown';


export function load(app: MarkdownApplication) {
  console.log('[typedoc-plugin-md-v2] load', app);
  // do something with app instance
}
